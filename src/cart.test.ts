import { Cart, Product, NotificationService } from './cart';

describe('Cart (TypeScript) – mocks e spies', () => {
  test('deve adicionar um item e chamar NotificationService.notify', () => {

  });

  test('deve remover um item do carrinho', () => {

  });

  test('deve calcular o total corretamente e usar reduce (spy)', () => {

  });

  test('deve limpar o carrinho', () => {

  });
});
